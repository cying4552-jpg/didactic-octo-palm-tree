ction addFocus(){let m=+focusMin.value||0;if(m<=0)return;state.focus[today()]=(state.focus[today()]||0)+m;focusMin.value='';save()}
function renderHome(){let now=new Date();todayText.textContent=now.toLocaleDateString('zh-CN',{month:'long',day:'numeric',weekday:'long'});helloText.textContent=now.getHours()<11?'早上好，先做最轻的一件事。':now.getHours()<18?'今天也一点点往前走。':'晚上好，给今天收个温柔的尾。';let td=state.tasks.filter(t=>!t.done&&t.date===today());statToday.textContent=td.length;statDDL.textContent=state.tasks.filter(t=>!t.done&&daysLeft(t.date)>=0&&daysLeft(t.date)<=3).length;let wd=weekDates().map(isoLocal),total=state.habits.length*7,done=state.habits.reduce((a,h)=>a+wd.filter(d=>h.checks[d]).length,0);statHabit.textContent=total?Math.round(done/total*100)+'%':'0%';statFocus.textContent=wd.reduce((a,d)=>a+(state.focus[d]||0),0);let high=state.tasks.filter(t=>!t.done&&['逾期','高风险'].includes(risk(t))).length;riskText.textContent=high?`${high} 个高风险`:'当前无高风险';todayList.innerHTML=td.length?td.map(t=>`<div class="item"><button class="ghost" onclick="toggleTask(${t.id})">○</button><div class="grow"><b>${esc(t.name)}</b><small>${esc(t.cat)} · ${t.minutes||0} 分钟</small></div><span class="badge">${risk(t)}</span></div>`).join(''):'<div class="notice">今天还没有待办。可以先加一件最小的事 🍑</div>';let cs=state.courses.filter(c=>c.day===now.getDay()).sort((a,b)=>a.start.localeCompare(b.start));todayCourses.innerHTML=cs.length?cs.map(c=>`<div class="item"><div class="grow"><b>${esc(c.name)}</b><small>${c.start}—${c.end}</small></div><span class="badge">课程</span></div>`).join(''):'<div class="notice">今天没有录入课程。</div>';let h='';for(let i=55;i>=0;i--){let d=new Date();d.setDate(d.getDate()-i);let k=isoLocal(d),score=(state.focus[k]||0)/25+state.habits.filter(x=>x.checks[k]).length,lv=score>=5?4:score>=3?3:score>=1?2:score>0?1:0;h+=`<div class="heat ${lv?'l'+lv:''}" title="${k}"></div>`}heatmap.innerHTML=h}
function renderPlan(){let a=[...state.tasks].sort((x,y)=>x.done-y.done||x.date.localeCompare(y.date));planCount.textContent=a.length+' 项';planList.innerHTML=a.length?a.map(t=>`<div class="item ${t.done?'done':''}"><button class="ghost" onclick="toggleTask(${t.id})">${t.done?'✓':'○'}</button><div class="grow"><b>${esc(t.name)}</b><small>${esc(t.cat)} · 截止 ${t.date} · ${t.minutes||0} 分钟 · ${esc(t.priority)}</small></div><span class="badge">${risk(t)}</span><button class="ghost" onclick="removeTask(${t.id})">×</button></div>`).join(''):'<div class="notice">还没有计划。</div>'}
function renderCourses(){const names=['周日','周一','周二','周三','周四','周五','周六'];courseCount.textContent=state.courses.length+' 门';courseGrid.innerHTML=state.courses.length?[...state.courses].sort((a,b)=>a.day-b.day||a.start.localeCompare(b.start)).map(c=>`<div class="course"><h3>${esc(c.name)}</h3><p>${names[c.day]} · ${c.start}—${c.end}</p><button class="ghost" style="margin-top:10px" onclick="removeCourse(${c.id})">删除</button></div>`).join(''):'<div class="notice">还没有课程。</div>'}
function renderHabits(){let ds=weekDates(),names=['一','二','三','四','五','六','日'];weekRange.textContent=`${ds[0].getMonth()+1}/${ds[0].getDate()} — ${ds[6].getMonth()+1}/${ds[6].getDate()}`;weekHead.innerHTML='<span>习惯</span>'+names.map(x=>'<span>周'+x+'</span>').join('');habitTable.innerHTML=state.habits.map(h=>`<div class="habit-row"><div><b>${esc(h.name)}</b></div>${ds.map(d=>{let k=isoLocal(d);return `<button class="day ${h.checks[k]?'on':''}" onclick="toggleHabit('${h.id}','${k}')">${h.checks[k]?'✓':''}</button>`}).join('')}</div>`).join('')}
function renderCalendar(){let n=new Date(),y=n.getFullYear(),m=n.getMonth();calTitle.textContent=`${y} 年 ${m+1} 月`;let start=(new Date(y,m,1).getDay()+6)%7,days=new Date(y,m+1,0).getDate(),h='';for(let i=0;i<start;i++)h+='<div></div>';for(let d=1;d<=days;d++){let k=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`,dateObj=new Date(y,m,d),taskN=state.tasks.filter(t=>!t.done&&t.date===k).length,courseN=state.courses.filter(c=>c.day===dateObj.getDay()).length;h+=`<div class="cal-day ${k===today()?'today':''}"><b>${d}</b><div class="dots">${taskN?'<i class="dot"></i>':''}${courseN?'<i class="dot" style="opacity:.45"></i>':''}</div>${taskN?`<small>${taskN} 待办</small>`:''}</div>`}calendarGrid.innerHTML=h}
function renderWeekly(){let ds=weekDates(),keys=ds.map(isoLocal),mins=keys.reduce((a,d)=>a+(state.focus[d]||0),0),doneTasks=state.tasks.filter(t=>t.done&&t.doneAt&&keys.includes(t.doneAt)).length,total=state.habits.length*7,hc=state.habits.reduce((a,x)=>a+keys.filter(d=>x.checks[d]).length,0),rate=total?Math.round(hc/total*100):0,high=state.tasks.filter(t=>!t.done&&['逾期','高风险'].includes(risk(t))).length;weekFocus.textContent=mins;weekDone.textContent=doneTasks;weekHabit.textContent=rate+'%';weekRisk.textContent=high;let max=Math.max(30,...keys.map(k=>state.focus[k]||0));focusChart.innerHTML=keys.map((k,i)=>`<div class="bar" style="height:${Math.max(5,(state.focus[k]||0)/max*100)}%"><span>${['一','二','三','四','五','六','日'][i]}</span></div>`).join('');let msg=mins===0?'这周还没有专注记录，可以从 15 分钟开始。':mins<180?'这周已经有投入了，下一步可以把专注分散到更多天，而不是集中在某一天。':'这周投入比较稳定，继续守住节奏比突然加量更重要。';if(high)msg+=` 目前还有 ${high} 个高风险任务，建议优先处理最临近截止的一个。`;if(rate>=70)msg+=' 习惯完成率也不错，可以少加新目标，多维持已经形成的节奏。';weeklyText.textContent=msg}
function renderHistory(){let months={};Object.entries(state.focus).forEach(([d,m])=>{let key=d.slice(0,7);months[key]??={focus:0,habit:0,tasks:0};months[key].focus+=m});state.habits.forEach(h=>Object.entries(h.checks).forEach(([d,on])=>{if(!on)return;let key=d.slice(0,7);months[key]??={focus:0,habit:0,tasks:0};months[key].habit++}));state.tasks.forEach(t=>{if(t.done&&t.doneAt){let key=t.doneAt.slice(0,7);months[key]??={focus:0,habit:0,tasks:0};months[key].tasks++}});let arr=Object.entries(months).sort((a,b)=>b[0].localeCompare(a[0]));historyGrid.innerHTML=arr.length?arr.map(([m,v])=>`<div class="history-card"><h3>${m}</h3><p>专注 ${v.focus} 分钟<br>习惯打卡 ${v.habit} 次<br>完成任务 ${v.tasks} 项</p></div>`).join(''):'<div class="notice">还没有足够的历史记录。开始记录后，这里会自动长出来。</div>'}
function rende