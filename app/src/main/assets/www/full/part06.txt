rThemes(){themeGrid.innerHTML=Object.entries(THEMES).map(([id,t])=>`<button class="theme ${state.theme===id?'active':''}" onclick="state.theme='${id}';save()"><b>${t.icon} ${t.name}</b><div class="swatches"><i style="background:${t.bg}"></i><i style="background:${t.panel2}"></i><i style="background:${t.accent}"></i><i style="background:${t.accent2}"></i></div></button>`).join('')}
function render(){applyTheme();renderHome();renderPlan();renderCourses();renderHabits();renderCalendar();renderThemes();renderWeekly();renderHistory()}
function exportData(){let blob=new Blob([JSON.stringify({app:'xiaotao-workbench-full',version:1,exportedAt:new Date().toISOString(),state},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='xiaotao-backup-'+today()+'.json';a.click();URL.revokeObjectURL(a.href)}
function importData(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{try{let o=JSON.parse(r.result),s=o.state||o;if(!Array.isArray(s.tasks)||!Array.isArray(s.habits))throw 0;if(confirm('导入会覆盖当前数据，继续吗？')){state=Object.assign(deepClone(defaults),s);save();alert('恢复成功')}}catch(err){alert('备份文件无效')}};r.readAsText(f)}
function resetData(){if(confirm('确定清空本地数据并恢复初始状态？')){state=deepClone(defaults);save()}}
function askAssistant(){let q=chatInput.value.trim();if(!q)return;chatBox.innerHTML+=`<div class="bubble me">${esc(q)}</div>`;chatInput.value='';let ans='';let low=q.toLowerCase(),high=state.tasks.filter(t=>!t.done&&['逾期','高风险'].includes(risk(t))),todayTasks=state.tasks.filter(t=>!t.done&&t.date===today());if(q.includes('先做')||q.includes('今天做')){let t=[...high,...todayTasks].filter((x,i,a)=>a.findIndex(y=>y.id===x.id)===i)[0];ans=t?`先做「${t.name}」。它现在是 ${risk(t)}，先给它 15～30 分钟，不要求一次完成。`:'今天没有明显高风险任务，可以先完成一个最小待办。'}else if(q.includes('高风险')||q.includes('ddl')||q.includes('DDL')){ans=high.length?`现在有 ${high.length} 个高风险任务：`+high.slice(0,4).map(x=>x.name).join('、')+'。':'目前没有高风险 DDL。'}else if(q.includes('课')){let now=new Date(),cs=state.courses.filter(c=>c.day===now.getDay());ans=cs.length?'今天的课：'+cs.map(c=>`${c.name} ${c.start}—${c.end}`).join('；')+'。':'今天没有录入课程。'}else if(q.includes('这周')||q.includes('学习怎么样')){let ks=weekDates().map(isoLocal),mins=ks.reduce((a,d)=>a+(state.focus[d]||0),0);ans=`这周已记录 ${mins} 分钟专注，当前有 ${high.length} 个高风险任务。更重要的是保持连续记录，不用追求某一天突然拉满。`;}else ans='我现在是本地规则版助手。你可以问：今天先做什么、有哪些高风险任务、这周学习怎么样、今天有什么课。';chatBox.innerHTML+=`<div class="bubble bot">${esc(ans)}</div>`;chatBox.scrollTop=chatBox.scrollHeight}
chatInput?.addEventListener('keydown',e=>{if(e.key==='Enter')askAssistant()})
render();
</script>
</body></html>